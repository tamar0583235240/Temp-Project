// import React from "react";
// import { useParams } from "react-router-dom";
// import PracticeQuestion from "../features/code-questions/components/PracticeQuestion";

// const PracticeQuestionPage: React.FC = () => {
//   const { questionId } = useParams<{ questionId: string }>();

//   if (!questionId) return <div>לא נבחרה שאלה</div>;

//   return <PracticeQuestion questionId={questionId} />;
// };

// export default PracticeQuestionPage;